# Copy to .env and fill in. Both are optional — the map works without them.

# Public Google Form URL (the "Suggest an initiative" button links here).
VITE_SUBMIT_URL=

# Apps Script web-app /exec URL that serves approved submissions as JSON.
VITE_SUBMISSIONS_URL=
